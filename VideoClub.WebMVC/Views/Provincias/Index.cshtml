@model List<VideoClub.Entidades.Entidades.Provincia>
@{ ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml"; }
<ol class="breadcrumb mt-4 mb-4 ">
    <li class="breadcrumb-item"><a href="index.html">Dashboard</a></li>
    <li class="breadcrumb-item active">Provincia</li>
</ol>
<div class="card">
    <h5 class="card-header bg-primary text-white">
        <i class="fas fa-map"></i>
        Listado de Provincias

    </h5>
    <div class="card-body">
        <a href="@Url.Action("Create","Provincias")" class="btn btn-primary"><i class="fas fa-plus"></i> Nuevo</a>
        <hr />
        <table class="table">
            <thead>
                <tr>
                    <th>Provincia</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count == 0)
                {
                    <tr>
                        <td>
                            No hay registros todavía
                        </td>
                    </tr>
 }
                else
                {
                    foreach (var provincia in Model)
                    {
                        <tr>    
                            <td>@Html.DisplayFor(modelItem => provincia.NombreProvincia)</td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = provincia.ProvinciaId }, new { @class = "btn btn-warning" })
                                @Html.ActionLink("Borrar", "Delete", new { id = provincia.ProvinciaId }, new { @class = "btn btn-danger" })

                            </td>
                        </tr>

                    }
                }
            </tbody>
        </table>

    </div>
    <div class="card-footer text-muted">
        Cantidad de registros: <strong>@Model.Count</strong>
    </div>
</div>
